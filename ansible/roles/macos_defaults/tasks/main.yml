---
# TODO Phase 5: osx_defaults loop over macos_defaults; notify killall Dock/Finder/SystemUIServer
- name: macos_defaults placeholder
  ansible.builtin.debug:
    msg: "macos_defaults role — TODO Build Phase 5"
